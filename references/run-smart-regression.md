# /testchimp run smart regression (legacy stub)

> **Renamed:** This workflow is now **`run-smart-smoke`**.
>
> **Read instead:** [`run-smart-smoke.md`](./run-smart-smoke.md)
>
> **Canonical prompt:** `/testchimp run smart smoke`
>
> **Legacy synonym (one release):** `/testchimp run smart regression` still routes here → follow **`run-smart-smoke`**. Prefer the new prompt and `workflow-id: run-smart-smoke` in plans, policies, and `report-agent-action`. For **`get-last-run-workflow-detail`**, prefer `run-smart-smoke`, then fall back once to `run-smart-regression`.

**Workflow id (legacy):** `run-smart-regression` → use **`run-smart-smoke`**.
