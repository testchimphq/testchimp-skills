# SAST (static code checks)

**Not implemented yet.** If `get-security-scan-config` returns **`detail.sastCheckConfig`**, tell the user that SAST release checks are coming soon, skip execution for this type, and let the parent orchestrator continue / complete appropriately.

Parent: [`../security_scans.md`](../security_scans.md).
