# Dependency scan

**Not implemented yet.** If `get-security-scan-config` returns **`detail.depsCheckConfig`**, tell the user that dependency scans are coming soon, skip execution for this type, and let the parent orchestrator continue / complete appropriately.

Parent: [`../security_scans.md`](../security_scans.md).
