# Importing existing tests (moved)

**This file moved.** Use the first-class import workflow playbook:

→ [`import-existing-tests.md`](./import-existing-tests.md) — workflow id **`import`** (`/testchimp import existing tests <folder>`).

Init Key Area 2 nests that playbook when an unmigrated suite is detected; see [`init-testchimp.md`](./init-testchimp.md).
