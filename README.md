# testchimp-skills

Cursor / Claude **agent skills** for TestChimp: `@testchimp /init`, `/test`, `/audit`.

- **`SKILL.md`** — entrypoint and routing
- **`init-testchimp.md`** — bootstrap repo + MCP + CI
- **`write-smarttests.md`** — SmartTest authoring
- **`audit-coverage.md`** — coverage and execution audit
- **`template_playwright.config.js`** — sample Playwright config

Pair with **`testchimp-mcp-client`** and set `TESTCHIMP_API_KEY`.

See [TestChimp](https://testchimp.io) for product docs.
